#include <stdio.h>
#include <stdlib.h>
void dfs(x,y);
int main()
{
    char a[100][100];
    int i,j,k[100][100];
    for (i=1;i<=n;i++)
    {
        for (j=1;j<m;j++)
        {
            gets(a[i][j]);
        }
    }

    return 0;
}
